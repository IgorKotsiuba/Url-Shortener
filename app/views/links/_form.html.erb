<%= form_for(@link) do |f| %>
  <% if @link.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :original_link %>
    <%= f.text_field :original, :placeholder => "Paste a link to shorten it", class: "form-control" %>
  </div>

  <div class="actions">
    <%= link_to 'Back', links_path, class: "btn btn-default" %>
    <%= f.submit "Shorten", class: "btn btn-primary" %>
  </div>

<% end %>
