<div class="jumbotron">
  <h1>UNLEASH THE POWER OF THE LINK </h1>
  <p> Welcome to our website! It’s a big a pleasure to see you. Choose the plan which is more suitable for you.</p>
  <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-info" %>

  <div class="row">
    <% @plans.each do |plan| %>
      <div class="col-sm-6 col-md-6">
        <div class="thumbnail">
          <div class="caption">
            <h3 class="plan"><%= plan.name %></h3>
            <p class="painment_info">each submission costs <%=number_to_currency plan.price %> </p>
            <div id="paypal_checkout">
              <%= link_to image_tag("https://www.paypal.com/en_US/i/btn/btn_xpressCheckout.gif"), paypal_checkout_path(plan_id: plan.id) %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
