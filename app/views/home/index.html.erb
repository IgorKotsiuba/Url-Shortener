<div class="jumbotron">
  <h1>UNLEASH THE POWER OF THE LINK </h1>
  <p> Welcome to our website! It’s a big a pleasure to see you. Choose the plan which is more suitable for you.</p>

  <div class="row" id="plan_list">
    <% @plans.each do |plan| %>
      <div class="col-sm-6 col-md-6">
        <div class="thumbnail">
          <div class="caption">
            <h3 class="plan"><%= plan.name %></h3>
            <p class="payment_info">each submission costs <%=number_to_currency plan.price %> </p>
            <div id="paypal_checkout">
              <% if user_signed_in? %>
                <%= link_to "Submit Order", edit_plan_path(plan), class: "btn btn-warning" %>
              <% else %>
                <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-info" %>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
